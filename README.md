# 📚 Library Desk Agent

AI-powered Library Desk Agent with chat interface.

## Setup
1. `pip install -r requirements.txt`
2. Copy `.env.example` to `.env` and add your API key
3. `python seed_db.py`
4. `python -m server.main`
5. Open http://localhost:8000